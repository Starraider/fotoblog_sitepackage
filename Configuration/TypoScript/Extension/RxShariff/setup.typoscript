tt_content.list.20.rxshariff_shariff {
    settings {
        enableBackend = 1
        data {
            lang = auto
            orientation = horizontal
            services = facebook,twitter,reddit,whatsapp,mail
            theme = grey
            twitter-via = sven_kalbhenn
            data-button-style = icon-count
        }
    }
}
